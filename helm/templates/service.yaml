apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-rabbit-exercise.fullname" . }}
  labels:
    {{- include "kube-rabbit-exercise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-rabbit-exercise.selectorLabels" . | nindent 4 }}
